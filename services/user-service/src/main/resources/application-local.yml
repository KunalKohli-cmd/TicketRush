spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5433/userdb
    username: ticketrush
    password: ticketrush
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379
server:
  port: 8081

app:
  jwt:
    secret: dGlja2V0cnVzaC1sb2NhbC1kZXYtc2VjcmV0LWtleS0zMmJ5dGVz
    issuer: ticketrush-user-service
    access-token-ttl: 15m
    refresh-token-ttl: 7d
